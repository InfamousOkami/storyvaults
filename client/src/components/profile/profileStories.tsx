"use client";

import { useState } from "react";

function ProfileStories() {
  const [isLoading, setIsLoading] = useState(true);
  const [stories, setStories] = useState();

  return <div>ProfileStories</div>;
}

export default ProfileStories;
